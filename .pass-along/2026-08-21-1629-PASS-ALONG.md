---
DATE: 2026-08-21T16:29:26-0400
TITLE: Beads goes ephemeral — three staged deliverables await the owner's go
STATUS: blocked
MODEL: claude-fable-5
MODEL_EFFORT: high
HARNESS: claude-code 2.1.228
SESSION_ID: 428d8d4f-5624-4682-9f0a-db75bc84fb40
REPO: amroja-biz/rockysurf
WORKING_DIR: /Users/johndamask/code/1-open-source-rocky-surf-v0-1
BRANCH: main (pass-alongs land here; work happens in worktrees)
HEAD_SHA: 622f632
UPSTREAM: origin/main
WORKTREE: clean
ISSUE_TRACKER: GitHub
ISSUES: ["#9 + #24 (comments staged)", "#2/#7/#24/#41/#42/#49/#50/#51 open (owner-filed)"]
TASK_TRACKER: Beads (being retired to ephemeral/local-only — see Decisions)
PRS: []
OPEN_LOOPS: ["bead-triage doc v2 awaiting owner review before 16 issues + 2 comments post (local: ~/scratch/rocky-surf-open-beads.md)", "docs-memories branch (6f2c027) unpushed — 7 migrated memories + new AGENTS.md/CLAUDE.md + CONTRIBUTING edit, awaiting owner review of the public/private classification", "skill/project-memories branch (f979588) in ~/code/john-claude-skills unpushed, plugin bumped to 1.5.12", "first bd gc purge deliberately NOT run — gated on the triage posting", "nightly real-cloud still failing (Aug 20, 21) at tool:build-essential — the jv0o re-decision is draft S1 in the triage doc", "stale node processes from older sessions still running (~3 besides the owner's live core)"]
RECOMMENDED_NEXT: Get the owner's review verdicts on the triage doc and the memory classification, then execute in order — post issues/comments, push docs-memories, push the skill branch, purge beads — because every one of those is finished work sitting in an unpushed staging area that a crash or cleanup could lose.
SCOPE_COMPLETE: false
PREDECESSOR: .pass-along/2026-08-21-1358-PASS-ALONG.md
PRIME: ["gh issue list --repo amroja-biz/rockysurf --state open", "git worktree list (run in the owner's old private checkout to find the staged memmig worktree)", "cat ~/scratch/rocky-surf-open-beads.md"]
---

## Start here

Everything is staged, nothing is posted: this session ended mid-review, with the owner
holding three go/no-go decisions. Read `## Waiting on a human` first — the session's entire
output is parked behind it. STATUS is `blocked` for exactly that reason.

## May have moved since I stopped

- **The owner may answer the three reviews at any time** (triage doc, memory classification,
  skill branch). If a later session sees the issues already posted or the branches pushed,
  the reviews happened — check `gh issue list` and the branch existence before redoing
  anything.
- **The nightly** (`nightly-real-cloud.yml`) was red Aug 20 and Aug 21 with the apt-mirror
  signature at `tool:build-essential`. Check the next morning's run before assuming state.
- **The owner's core restarted onto current main** — a process now serves :3000 from
  `~/code/rockysurf` at `a834565`, which includes all four of yesterday's merged PRs and both
  DB migrations. The "stale runtime" loop from the predecessor is CLOSED.

## What this session did (since the 13:58 predecessor)

- **Settled the beads question** with the owner, informed by a research pass over the beads
  docs (gastownhall/beads): GitHub issues are the system of record; beads becomes optional,
  personal, local-only working memory with NO git sync anywhere, purged periodically
  (`bd gc`). Sensitive-leak risk is closed structurally — a DB that never touches a remote
  cannot publish anything. The archived private repo stays archived.
- **Pass-alongs moved to this repo** (owner ruling): first entry + this one, in
  `.pass-along/`. World-readable — the no-secrets rule is load-bearing.
- **Three deliverables staged by parallel subagents, all unpushed pending review:**
  1. **Bead triage** — 36 non-closed beads → 16 draft issues + 2 comments (on open #9 and
     #24), consolidated along DB-verified parent/child edges (launch epic absorbs 8 beads;
     v0.2 roadmap absorbs 6 — the latter grouping editorial). 5 drops, 1 covered. Doc for
     review: `~/scratch/rocky-surf-open-beads.md` (v2). Nothing created/closed/modified yet.
  2. **Memory migration** — 29 `bd remember` entries classified 8 PUBLIC / 11 PRIVATE / 10
     DEAD; the public ones written as `docs/memories/` + `llms.txt`, plus NEW `AGENTS.md`
     (tracking policy) and `CLAUDE.md` (pointer) and a tightened CONTRIBUTING "Issue
     tracking" section. Branch `docs-memories`, commit `6f2c027`, in a worktree (find via
     `git worktree list` from the old private checkout).
  3. **`project-memories` skill** — new skill in the owner's `john-claude-skills` repo
     (branch `skill/project-memories`, commit `f979588`, auto-bumped to 1.5.12): the
     llms.txt memories pattern as a reusable skill, sensitivity rule prominent.
- **Found two triage traps worth keeping**: old-repo issue references in beads ("#1"/"#3"/
  "#4") are a different numbering space — on this repo those numbers are merged PRs; and
  this repo's open #2 is idle-stopping/guardrails, NOT the old repo's "loud launch" #2.

## Commits

`622f632` on main (predecessor pass-along; pushed). Nothing else — all session output is on
the three unpushed branches above.

## Open threads

- **Triage posting** (in scope, unfinished — blocked on owner): 16 issues + comments on #9
  and #24 per the review doc. Done = issues exist, comments posted, doc archived.
- **docs-memories push** (in scope, unfinished — blocked on owner's classification review):
  done = branch pushed/merged; the 11 PRIVATE memories stay in local bd; the 10 DEAD ones die
  in the purge.
- **Skill push** (in scope, unfinished — blocked on owner): done = branch pushed to
  `jbdamask/john-claude-skills` and merged/installed at the owner's pleasure.
- **First `bd gc` purge** (in scope, deliberately sequenced last): must NOT run before the
  triage posts — the purge is what makes bead loss irreversible. Constraint, not preference.
- **jv0o / red nightly** (carried): the re-decision is now draft S1 in the triage doc, with
  the two CI failures as evidence. Posting it puts the decision on GitHub where it belongs.
- **Stale processes** (carried housekeeping): besides the owner's live core on :3000, ~3
  older `dist/bin.js` node processes from previous sessions linger; likely safe to kill,
  confirm with the owner.
- Carried untouched: launch gates (in the triage doc's epic 1), Pack Shop nav ruling — note
  owner-filed open **#51 "Consolidate Surge Pack and Pack Shop"** may BE that ruling's issue
  now.

## Recommended next — this session's view

Collect the owner's three review verdicts and execute the staged work in this order: post
the triage issues/comments → push docs-memories → push the skill branch → run `bd gc
--dry-run`, show the numbers, purge. Reasoning: all four are finished work parked in
fragile places (a scratch doc under `~/scratch`, two unpushed branches — one of them in a
session-scoped worktree that a cleanup could remove). The longer they sit, the more likely
GitHub state drifts under the triage drafts (the owner is actively filing issues — #49–#51
appeared recently). Consequence of skipping: the beads purge either waits (fine) or, if run
early, destroys the only source the drafts were built from.

## Stated direction

- The owner: beads are ephemeral once the PR merges and the issue closes; "best practice
  from Steve Yegge is to periodically purge"; the project should be beads-agnostic so
  contributors can use it or not — personal beads.
- The owner: pass-alongs live in this repo, current-and-future only; "we can discuss beads"
  separately (that discussion then produced the policy above).
- The owner asked for bead→epic consolidation grounded in actual bead relationships, and:
  "if a bead is still viable and its gh issue is still open, simply add the bead info to a
  comment on the respective issue."

## Decisions and rules

- **GitHub is the system of record; beads is personal scratch.** Never re-point beads sync
  at any remote, never commit `.beads/` to this repo. The beads DB lives locally (its
  workspace home is the old private checkout) and gets `bd gc` purges.
- **Pass-alongs: this repo, `.pass-along/`, public.** Scrub before commit (grep for IPs,
  12-digit account ids, token patterns). Older pass-alongs stay in the archived private repo
  deliberately.
- **Memories: `docs/memories/` llms.txt pattern for publishable knowledge; local bd for
  sensitive.** Never half-scrub a sensitive memory into the public repo — leave it out.
- **Old-repo issue numbers cited inside beads are meaningless here** — different numbering
  space; verify against this repo before trusting any "#N" reference.

## Traps

- The staged `docs-memories` worktree lives under a SESSION-SCOPED scratchpad path —
  discoverable via `git worktree list` from the old private checkout, but a scratchpad
  cleanup could delete it. If it's gone, the branch `docs-memories` may survive in the old
  checkout's refs; check there before rebuilding.
- `bd` resolves its workspace to the old private checkout (`~/code/rockysurf-open`), not to
  whatever directory you run it in. `bd dolt push` fails by design now (archived remote) —
  not a bug.
- This monorepo needs node 24 (`nvm use 24`) — corepack pnpm crashes on node 20. Fresh
  worktrees need `pnpm install` AND `pnpm -r build` before tests; `bundle-assets.test.ts`
  failures mean "you didn't build".
- Subagents in this setup sometimes go idle WITHOUT delivering their final report — nudge
  them via SendMessage and the report arrives; don't respawn.

## Waiting on a human

1. **Triage doc verdict** (`~/scratch/rocky-surf-open-beads.md`): "post them" or edits —
   16 issues + 2 comments are behind it, and the purge is behind those.
2. **Memory classification verdict** (8 PUBLIC / 11 PRIVATE / 10 DEAD table shown in
   session): push of `docs-memories` (and the repo's first AGENTS.md) is behind it.
3. **Skill branch verdict**: push of `skill/project-memories` to their skills repo.
4. Carried: jv0o re-decision (in the triage doc), stale-process cleanup nod.

## Housekeeping

- Live and correct: the owner's core on :3000 from `~/code/rockysurf` at `a834565`.
- Lingering: ~3 older node `dist/bin.js` processes from prior sessions (one since Aug 12) —
  safe to kill after owner nod.
- Worktrees: `memmig` (docs-memories) and `pa2` (this file) under the session scratchpad;
  `pa2` is removable once this is pushed; `memmig` must survive until its branch is pushed.
- `~/scratch/rocky-surf-open-beads.md` is the only copy of the consolidated triage — don't
  clean it up until posted.
