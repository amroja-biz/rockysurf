---
DATE: 2026-09-06T18:31:54-0400
TITLE: Afternoon wave — npm trusted publishing shipped (#275), the Rocky Surf Shop tab returned (#426), MCP responses made concise (#415/#416), README quick start (#414), docs cleanup with the spike moved to history (#339), DO stop-modal copy (#413), the owner's provider-doc revision; v0.1.0 tag and first CI publish still ahead
STATUS: milestone_complete
MODEL: claude-fable-5-1
MODEL_EFFORT: medium
HARNESS: claude-code 2.1.261
SESSION_ID: dfcec153-0fd1-424b-babb-44a6ba1485f1
REPO: amroja-biz/rockysurf
WORKING_DIR: /Users/johndamask/code/rockysurf
BRANCH: main
HEAD_SHA: b9d7c59
UPSTREAM: origin/main (in sync, 0/0)
WORKTREE: clean (no agent worktrees; owner's checkout pulled to b9d7c59)
ISSUE_TRACKER: GitHub
ISSUES: ["closed this session (afternoon): #275 (#427), #426 (#428), #414 (#430), #339 (#431), #415+#416 (#432), #413 (#433), #420 (#424), #372 #373 #375 #376 #377 (verified, closed on the owner's word)", "filed this session, open: #435 (docs audience: delete the dead e2e checklist, tag every doc, split the pack contract, move TESTING/RELEASING)", "inherited/open: #364 #337 #315 #261 #253 #243 #167 #166 #165 #136 #134 #133 #131 #130 #123 #104 #103 #102 #101 #98 #78 #77 #76 #75"]
TASK_TRACKER: Beads
BEADS: ["0 open — nothing created; every issue this wave was single-PR (below the threshold in docs/memories/2026-08-26-orchestrating-issue-agent-teams.md)"]
PRS: ["none open on rockysurf; rockysurf-shop #22 merged (README: in-app install)"]
OPEN_LOOPS: ["v0.1.0 is NOT tagged or published: `npm view rockysurf version` is still 0.0.1, no v* tags, no 'Release to npm' run yet. The owner said they would tag after the Shop tab merged; it merged. The one-time bootstrap (npm login, `node scripts/npm-bootstrap-trust.mjs`) must run BEFORE the tag, or the workflow's publish is refused — steps are in the #275 comment and docs/RELEASING.md", "The first CI publish is the real test of two things no one has exercised: pnpm 11.21's OIDC publish (11.0.8 had a regression, pnpm/pnpm#11513) and the `npm trust list --json` shape the bootstrap script's already-configured check assumes (a wrong guess only re-runs `npm trust`, harmless)", "After the first publish: docs/RELEASING.md 'Retire the pre-publish notes' — the 'Not published yet' text in docs/self-hosting.md and elsewhere becomes false; unfiled", "#435 (docs audience) filed, not started — the owner said 'add this as a gh issue'; no agent launched", "Both new contribute-* skills (#419, #420) have never opened a real fork PR on the shop; their references/acceptance.md describe the live run — still owed", "Shop repo: README 'Installing one' and CONTRIBUTING now describe in-app install and the generator; the shop's own providers.json lists @rockysurf/provider-digitalocean 0.1.0 pointing at the owner's release tag provider-digitalocean-v0.1.0 — that tag/release is REAL, keep it", "The owner's server: no `packages/rockysurf/dist/bin.js` server process was running at 18:31 EDT (only Claude's MCP client, pid 48108); ~/.rockysurf/config.yaml has a providers.digitalocean section again. The owner said they were 'tweaking a couple of things' — do not start or restart it for them", "The bootstrap apt-retry test times out under local CPU load only (5 s budget; passes alone) — seen by the #426 agent, CI green; unfiled", "The MCP `list_offerings` now excludes nothing by region at the MCP layer on purpose (the SDK contract puts that in providers); if an operator still sees ~100 'not sold here' DigitalOcean rows, that is the in-tree provider's listOfferings, not the MCP", "Carried: minted local API token to revoke; #315 flip; drizzle-kit db:generate broken since #356 (unfiled); AWS SSO expired here; three failed DO rows in the owner's DB; stash 'On fix/ui-audit-packs'; grok/Amp sessions unexamined"]
RECOMMENDED_NEXT: Run the npm bootstrap and tag v0.1.0 — everything else this week was built so that publish can happen from CI, and until it runs, the trusted-publishing design, the pnpm OIDC path and the `npx -y rockysurf` line at the top of the new README are all unproven.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-09-06-1209-PASS-ALONG.md
PRIME: ["cd ~/code/rockysurf && git pull && git log --oneline -12", "gh issue view 275 --comments | tail -40   # the owner's one-time npm steps", "npm view rockysurf version; git ls-remote --tags origin | grep v0   # has v0.1.0 happened?", "cat docs/memories/llms.txt", "gh issue view 435"]
---

## Start here

Main is `b9d7c59`, clean, in sync, no PRs open, no worktrees. Ten PRs landed this afternoon, all
merged by the owner except the two `--auto` docs ones. The next real event is the owner's
v0.1.0 tag, which triggers the first CI publish to npm. Predecessor:
`.pass-along/2026-09-06-1209-PASS-ALONG.md` (the morning: UAT #375/#376, shop walkthrough,
#418/#419/#420, beads threshold).

## May have moved since I stopped

- **v0.1.0.** If `git ls-remote --tags origin | grep v0.1.0` shows the tag, check Actions →
  "Release to npm" for the run and its environment approval, then `npm view rockysurf version`
  and `npm view @rockysurf/core version`. If the run failed at the publish step with an auth
  error, suspect in order: the bootstrap script not run (no trusted publisher on the package),
  the `npm` environment name mismatch, pnpm's OIDC path (compare with `npm publish --provenance`
  per package as a diagnostic, not a fix — RELEASING says never `npm publish`).
- **The owner's server** was down at 18:31 EDT and the config gained a `providers.digitalocean`
  section again; the owner was tweaking things by hand.
- **The shop** may have new PRs from the owner exercising the contribute-* skills.

## What this session did (afternoon; the morning is in the predecessor)

1. **#275 → PR #427**: `.github/workflows/release.yml` (v* tag, `npm` environment with the owner as
   required reviewer, is-on-main + version-matches-tag checks, full gate, build, verify-tarballs,
   `pnpm publish -r --provenance` under OIDC, then `npx rockysurf@<ver> --version` from the real
   registry); `scripts/npm-bootstrap-trust.mjs` (0.0.0 placeholders where a package does not
   exist, `npm trust github`, `mfa=publish`); RELEASING.md and SECURITY.md sections. The GitHub
   environment was created via the API from this session. Owner merged; #429 records the npm org exists.
2. **#426 → PR #428 (Fable agent)**: the "Rocky Surf Shop" tab (`/shop`) installs community packs
   and providers in-app; #380's installer restored from `27a083c^`; refuses runtime deps; ADR-0028
   second amendment framed as "#394's naming and placement stand, only link-instead-of-install is
   withdrawn". 51 Playwright tests incl. real DO tarball install → restart → Settings panel.
   Shop README updated (shop #22).
3. **#415+#416 → PR #432 (Opus)**: `list_providers` 148,485 → 2,118 bytes; `list_offerings`
   paged (limit 100/max 500, cursor, `available_only`); #416's cause was the servers route never
   reading `?includeTerminated`; `list_servers` rows trimmed to the fleet view.
4. **#414 → PR #430 (Opus)**: README leads with Quick Start (install, official clouds, MCP).
   Correction learned: the MCP token is minted only by `rockysurf token`; Settings → MCP edits scopes.
5. **#339 → PR #431 (Opus)**: 23 deletions (SPEC-P1/P2, learnings/aws.md, 20 pass-alongs); the
   seven spike files MOVED to `docs/history/spike/` after the owner's review (ADRs 1–4 and the
   bootstrap contract key their amendment ids to the findings memo); prune rule recorded in the
   pass-along memory.
6. **#413 → PR #433 (by hand)**: both stop modals carry the owner's sentence about which clouds
   do not charge for stopped servers.
7. **#434**: the owner's revised `docs/writing-a-provider.md` dropped in verbatim (imperative
   headings; anchors changed; nothing in either repo linked to the old anchors).
8. **#435 filed**: the docs-audience assessment (delete e2e checklist; audience line per doc;
   split pack guide/contract; move TESTING/RELEASING under docs/contributing; trims as later issues).
9. Five DO UAT issues closed as verified; four agent worktrees + the #413 one removed.

## Commits

Ten since `f09deec`, all via PR: `ea4d58c` #424, `9081b5a` #425, `0974811` #427, `a726cea` #429,
`a9fd2ce` #428, `7b28840` #434, `4f90935` #430, `714777f` #431, `d78b5af` #432, `b9d7c59` #433.

## Open threads

Neutral inventory; recommendation follows.

- **v0.1.0 tag + first CI publish** — in scope of the week, owner's hands: bootstrap script,
  then tag, then approve. Not done as of 18:31 EDT.
- **Post-publish note retirement** — RELEASING.md lists the files; becomes a docs PR the moment
  `npm view rockysurf version` says 0.1.0.
- **#435** — filed, unstarted; an Opus docs agent's afternoon. The self-hosting trim inside it
  is deliberately a separate later issue.
- **Live acceptance runs of contribute-surge-pack / contribute-provider** — owed since morning.
- **Unfiled small things**: the apt-retry test's 5 s budget under load; drizzle-kit generate broken.
- **Carried**: minted token; #315; AWS SSO; failed DO rows; stash; old grok/Amp sessions.

## Recommended next — this session's view

I'd do the npm bootstrap and tag v0.1.0 before anything else, because the new README's first
line is `npx -y rockysurf`, the shop lists a provider whose install path assumes a published SDK
for `rockysurf-shop-entry`, and the trusted-publishing design has never been exercised end to
end. If the first publish fails, the failure is cheap now and expensive after a launch
announcement. Second, #435, because the owner raised it and the dead checklist is a one-line delete.

## Stated direction

- "I'll wait for the shoptab to finish before tagging and publishing" — it finished and merged.
- "add this as a gh issue, then write a pass-along, commit and push" — #435 and this file.
- The owner is tweaking things on their own machine concurrently; they pulled main to `b9d7c59`.

## Decisions and rules

- **Beads threshold** (memory, 2026-09-06): no beads for single-PR issues; an epic with deps for
  multi-PR or ordered work; `bd` shares the main DB with worktrees, no symlink.
- **#394 stands except its third clause**: the tab is Surge Packs; providers are configured in
  Settings; Rocky Surf now installs providers and community packs from its own Shop tab.
- **Publishing is CI-only**: a local `pnpm publish` is not a fallback; fix the workflow instead.
  Placeholders at `0.0.0` are the accepted bootstrap for trusted publishing on new names.
- **One agent for two issues in one file** (#415/#416, both in `mcp/tools.ts`), one commit each.
- **Spike evidence lives in `docs/history/spike/`**, not deleted: the bootstrap contract's
  amendment ids resolve there.
- **The owner's checkout**: agents never build there; this session edited docs and workflows in
  it on branches and pushed; that was fine because the server was not rebuilt.

## Traps

- **`npm trust` needs npm ≥ 11.15 and a package that exists**; the script checks the first and
  works around the second. Node 24 on this machine ships npm 11.17.
- **`actions/setup-node` with `registry-url` writes an `_authToken` line that breaks OIDC**; the
  release workflow deliberately omits it.
- **The servers API ignored `?includeTerminated`** and the SPA's client-side filter hid it; a tool
  flag that "does nothing" may be the route, not the tool.
- **A heredoc python step is not part of a `&&` chain** — put the whole thing in one script.
- **`SendMessage` to an agent can be blocked by the classifier** when the message carries a
  URL + hash; plain prose goes through.
- **`.git/index.lock` collisions** with agent worktrees: wait, do not delete.
- **`rockysurf token --help` mints a token.** Unchanged.

## Waiting on a human

1. npm bootstrap + `v0.1.0` tag + environment approval (the #275 comment has the commands).
2. After publish: retire the pre-publish notes (docs PR).
3. #435 go/no-go for an agent.
4. Live acceptance runs of the two contribute-* skills.
5. Revoke the minted local API token (carried).

## Housekeeping

- **Worktrees**: none. **Beads**: 0 open. **Stash**: `On fix/ui-audit-packs` still there.
- **Scratchpad** (`/private/tmp/claude-501/-Users-johndamask-code-rockysurf/dfcec153-…/scratchpad`):
  `AGENT-BRIEF.md` (updated through the afternoon wave — copy forward), `uat375/rockysurf-shop`
  (the shop clone used for every shop PR today, on `main`), `uat376/` (safe to delete), applied
  shop text files, `skill-issues.txt`.
- **GitHub environment `npm`** on rockysurf: required reviewer jbdamask, tag policy `v*`.
- **Notion**: the two #375 tasks in Rocky Surf Human Tasks are effectively done; not updated.
