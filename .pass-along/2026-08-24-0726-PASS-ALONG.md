---
DATE: 2026-08-24T07:26:29-0400
TITLE: The copy night — six PRs (#82–#87), the hero rewritten three times, memories migrated, skill baked, nightly green three mornings
STATUS: milestone_complete
MODEL: claude-fable-5
MODEL_EFFORT: high
HARNESS: claude-code 2.1.228
SESSION_ID: a7df88c0-23b0-4815-ac12-4fca9bc87dc1
REPO: amroja-biz/rockysurf
WORKING_DIR: /Users/johndamask/code/1-open-source-rocky-surf-v0-1
BRANCH: main (pass-alongs land here; all work happened in session-scratchpad worktrees, all removed)
HEAD_SHA: d4c1ea0
UPSTREAM: origin/main
WORKTREE: clean
ISSUE_TRACKER: GitHub
ISSUES: ["#2/#7/#9/#72/#75/#76/#77/#78 open, none touched this session"]
TASK_TRACKER: Beads (ephemeral/local; used for commit-subject attribution only)
BEADS: ["created+closed this session: y3g1, vn90, on23, 2jm6, gg3q, t1pf", "the 16:29 triage doc still supersedes the old open beads"]
PRS: []
OPEN_LOOPS: ["bead-triage doc (~/scratch/rocky-surf-open-beads.md) STILL unposted — last survivor of the 16:29 trio; needs a prune first (the Aug-21 farm built ~10 of its 16 drafts) and the first bd gc purge stays sequenced behind its posting", "owner's live core on :3000 unconfirmed against current main — d4c1ea0 carries six copy PRs plus the owner's own README edit; landing page invisible until pull+rebuild+restart", "jv0o: nightly went GREEN Aug 22/23/24, so the two red mornings were a transient mirror hiccup — the re-decision urgency is gone but the apt-retry hardening itself is still unbuilt (draft S1 in the triage doc)", "launch gates unchanged: m390 npm account, o45s.2 stranger test, o45s.3 v0.1.0, 31du CI secrets/OIDC", "issues #72 (Agent Deck removal) and #75–#78 (docs standards, design pass) open, unbuilt", "~3 stale node dist/bin.js processes from pre-Aug-21 sessions still lingering (cleanup needs owner nod)", "old archived checkout cruft: untracked rockysurf-check/ clone and a staged-but-uncommitted copy of the 13:58 pass-along (superseded — the same file is committed here)"]
RECOMMENDED_NEXT: Prune and post the bead-triage doc — it is the last unshipped deliverable from 2026-08-21, it goes staler with every session that builds beads it triaged, and the bd gc purge (which makes bead loss irreversible) is deliberately blocked behind it.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-21-2005-PASS-ALONG.md
PRIME: ["gh pr list --repo amroja-biz/rockysurf", "git log --oneline -8", "cat ~/scratch/rocky-surf-open-beads.md"]
---

## Start here

Main is at `d4c1ea0`, all CI green, zero open PRs. This session (the night of Aug 21–22)
was almost entirely front-door copy — the README and landing page were rewritten, tightened,
and re-voiced three times under live owner direction — plus two of the three staged
deliverables from the 16:29 session shipped. The devlog narrative stops before this session;
the seams are all here.

## May have moved since I stopped

- **The owner's live core** (`~/code/rockysurf`, :3000): unconfirmed whether it has been
  restarted onto anything ≥ `d4c1ea0`. Until it is, the app shows none of the copy work.
  Check the running build before assuming what the owner sees.
- **The owner edits the README directly on main** (commit `a8ba0f3`, "tweaks"). If they did
  it again after `d4c1ea0`, the landing page is out of sync again — the same-text contract
  is enforced by tests only when someone edits through a PR. `git log main -- README.md`
  first, and mirror any new direct edits into `HomePage.tsx` (PR #87 is the template for
  that chore).
- **The nightly** was verified GREEN three mornings running (Aug 22, 23, 24 — ~10 min runs,
  vs 24 min when failing). That is checked fact as of this writing, not a guess.

## What this session did

Six PRs, all merged to main with 27/27 checks each; every commit subject carries its bead id.

- **PR #82** (`5e271c5`, bead y3g1): full README + landing rewrite to one shared text —
  what-it-is/BYO*/principles up top, then install, creating a server (provider console-login
  caveat, Surge Packs, GitHub via Settings), where state lives + backup tips, cloud-side
  safeguards. Added the drift-guard tests (h2 spine in order, five principles present) so
  the two copies cannot silently diverge. Written by an Opus subagent.
- **PR #83** (`8b88475`, bead vn90): concision pass — README prose −32%, HomePage −24%.
  Owner verdict on #82 was "way too verbose". Also Opus, at the owner's explicit direction
  ("that model is less cringy than newer ones").
- **PR #84** (`e429b54`, bead on23): the 16:29 session's memory migration, landed. 7
  public-safe `bd remember` entries as `docs/memories/` + `llms.txt`, the repo's first
  `AGENTS.md`/`CLAUDE.md` (GitHub is the system of record; beads is personal scratch),
  CONTRIBUTING tightened. Diff re-scanned for secrets/IPs/account-ids before push — clean.
- **PR #85** (`ebe9eb4`, bead 2jm6): dropped the opening tagline ("a real Linux server that
  stays put") — owner ruled it pitch-speak. Opened with the facts instead.
- **PR #86** (`f4a65af`, bead gg3q): owner-dictated hero, verbatim — the "need their own
  space?" question h1, the snooping/API-keys/laptop paragraph, the solves-it paragraph. One
  editorial fix: "reinstalled" → "preinstalled".
- **PR #87** (`d4c1ea0`, bead t1pf): mirrored the owner's direct README edit (`a8ba0f3`)
  into the landing page — "Rocky Surf Principles" retitle trimmed to the five bold lines,
  install-section trims, Surge Pack rewording. Same-text contract restored.
- **The project-memories skill shipped** to `jbdamask/john-claude-skills` main
  (`b9fc40a..493b2b4`, plugin 1.5.13): the skill itself, a new "Memory, ADR, or doc?"
  routing section (memories = intrinsic learned knowledge; ADRs = choices among
  alternatives; docs = the system as it is; memories graduate to those homes when they turn
  normative), and an Opus prose rewrite.
- The owner merged **PR #58** (OpenClaw wallpaper) themselves at the session's start —
  the trademark hold is resolved.

## Commits

Six merge commits on this repo's main since the predecessor (`74336b1`/#58 owner-merged,
then #82–#87 as listed above), plus the owner's direct `a8ba0f3` and this file. On the
skills repo: three commits fast-forwarded to main. Nothing unpushed anywhere.

## Open threads

Neutral inventory; recommendation follows.

- **Bead-triage doc** (inherited, the last of the 16:29 trio): `~/scratch/rocky-surf-open-beads.md`
  is still the only copy. It predates the Aug-21 farm, which built roughly 10 of its 16
  drafts — it needs a prune before posting, then 16-minus-built issues + comments on #9/#24
  land on GitHub. Done = posted and the doc archived. The first `bd gc` purge is
  deliberately sequenced after that posting (constraint, not preference: the purge is what
  makes bead loss irreversible).
- **Owner core restart** — blocked on the owner, as always (owner-runs-their-own-core rule).
- **jv0o** (carried, urgency now reduced): three green nightlies ended the emergency, but
  bootstrap still has no apt retry — a launch-day stranger's create still dies if a mirror
  hiccups at the wrong moment. The fix shape lives on the bead and in triage draft S1.
- **Issues #75–#77** (docs standards) and **#78** (design pass): open, unbuilt, unchanged.
  **#72** (remove Agent Deck from packs): owner-filed, untouched.
- **Launch gates** (inherited, unchanged): m390 npm account, o45s.2 stranger test, o45s.3
  v0.1.0 + launch post, 31du CI secrets/OIDC.
- **Stale processes / old-checkout cruft**: see Housekeeping.

## Recommended next — this session's view

Prune and post the triage doc. It is finished work from three days ago sitting in a scratch
file, it is the only thing still blocking the beads purge, and its drafts rot as sessions
build the beads they triaged — the farm already invalidated more than half of it, and every
further session widens that gap. The prune is mechanical: check each draft against merged
PRs and closed beads, drop what shipped, post the rest. Consequence of skipping: the doc
eventually diverges enough that someone re-triages 36 beads from scratch, and the purge
stays blocked meanwhile. After that, #75 (docs standards study) remains the natural next
build, anchored on CORE-PRINCIPLES.md.

## Stated direction

- The owner, on voice: "I want the vibe of Rocky surf to be more blue collar than that.
  Just say what it is" — and "good writing is clear and concise." That ruling produced
  #85/#83 and should govern all future front-door copy.
- The owner, on models for copywriting: use Opus ("that model is less cringy than newer
  ones") — they asked for it by name twice for prose work.
- The hero copy in #86 is owner-dictated **verbatim**. Do not copyedit it in a future pass
  without asking.

## Decisions and rules

- **The README/landing same-text contract is now test-enforced** (`HomePage.test.tsx`: h1
  pins the tagline, h2 spine in order, five principles present). A copy change that touches
  one file and not the other fails the build — that is the feature working, not a flaky
  test. The owner's direct-to-main README edits bypass it; syncing those is a recurring
  chore (PR #87 is the pattern).
- **Ephemeral-beads attribution flow, demonstrated all session**: `bd create` from the main
  checkout → bead id as commit-subject prefix → `bd close` with the merge sha after. Two
  subagent-created beads (y3g1, vn90) were found still open days later and closed during
  this pass-along — subagents reliably create beads but forget to close them; sweep for
  that at session end.
- **Memory vs ADR vs doc routing** is now written down in the project-memories skill
  (1.5.13): route before writing, and never let a memory fork a normative document.

## Traps

- **Two remotes, two names.** Worktrees cut from `~/code/rockysurf` push to `origin`;
  the old archived checkout and its worktrees use `public-launch` for the same repo. A push
  to the wrong name fails with a misleading access-rights error (cost one failed PR create
  this session).
- **Subagents go idle without delivering their final report — routinely.** The nudge
  sometimes produces nothing but another idle ping. Faster and more reliable: verify ground
  truth directly (`gh pr list`, branch existence, worktree log) instead of nudge-looping.
- Carried, all still true: node 24 for pnpm (`nvm use 24`); fresh worktrees need
  `pnpm install` AND `pnpm -r build` before tests; bd resolves its DB to the old private
  checkout (run it from there or the main checkout, never `bd -C`); shell cwd resets
  between Bash calls; the dcg guard blocks `rm -rf` and `git reset --hard` — use
  `git worktree remove` and unique dirs.

## Waiting on a human

1. **Triage-doc verdict** — read `~/scratch/rocky-surf-open-beads.md`, or say "prune and
   post" and an agent does the rest. The purge is queued behind it.
2. **Core restart** onto current main, so the copy work is visible in the app.
3. **Launch gates**: npm account (m390), stranger-test recruits (o45s.2), the v0.1.0 nod.
4. Cleanup nods: the ~3 stale node processes; the old checkout's `rockysurf-check/` clone
   and its superseded staged pass-along copy.

## Housekeeping

- All five of this session's scratchpad worktrees (copywrite, copytighten, openingline,
  tagline, tweaksync) are removed and their branches deleted; the pass-along worktree `pa`
  goes last, after this file is pushed. A harmless `draft.md` scratch remains in the
  session scratchpad.
- The old archived checkout (`WORKING_DIR` above) still holds: an untracked
  `rockysurf-check/` gitleaks-verification clone (safe to delete on owner nod) and a staged
  copy of the 13:58 pass-along — superseded, since the identical file is committed in THIS
  repo; safe to unstage/discard on owner nod, never to commit (the archive is read-only
  anyway).
- Beads: six beads created and closed this session (list in frontmatter); the DB remains
  laptop-only by design; `bd dolt push` still fails by design.
