---
DATE: 2026-09-08T11:36:41-0400
TITLE: Post-publish day — v0.1.0 release notes drafted, release SOP written and reviewed (#465), the four primitives ruled and swept (#464/#468/#469), quick start re-tested on npm, then the owner's own e2e found `npx -y rockysurf` broken on Node 20 (#470, fix #471 green, unmerged); v0.1.1 is the next step
STATUS: in_progress
MODEL: claude-fable-5-1
MODEL_EFFORT: medium
HARNESS: claude-code 2.1.263
SESSION_ID: a5625b2d-5745-4161-9a6c-f85d3e2b3cdc
REPO: amroja-biz/rockysurf
WORKING_DIR: /Users/johndamask/code/rockysurf
BRANCH: main
HEAD_SHA: 79e9925
UPSTREAM: origin/main (in sync, 0/0)
WORKTREE: clean (sweep agent worktree removed at write time)
ISSUE_TRACKER: GitHub
ISSUES: ["filed this session: #463 (help over MCP, from #243), #464 (primitives memory + sweep; owner ruled Provider, Surge Pack, Tool, Server; memory and sweep merged, issue still open), #466 (agent-quickstart defects from the dry run), #467 (SSH allow-list field help contradicts the API), #470 (npx on Node 20 runs the 0.0.1 placeholder)", "inherited/open: #130 (v0.1.0 launch checklist), #243, #437 #438 #453 #459 #315 #337 #261 and the older list in the predecessor"]
TASK_TRACKER: Beads
BEADS: ["0 open; none created this session (every item was single-PR)"]
PRS: ["#471 OPEN and GREEN (engines fix for #470) — the owner said they would merge; check", "merged this session: #465 release SOP, #468 primitives memory, #469 primitives sweep"]
OPEN_LOOPS: ["v0.1.1 release: owner said 'we are shipping today'. Sequence per docs/RELEASE_SOP.md: merge #471 → bump every package to 0.1.1 on a release branch → PR → merge → annotated tag v0.1.1 on origin/main → push → owner approves the `npm` environment → workflow publishes → verify `npx -y rockysurf@0.1.1 --version` under Node 24 (prints 0.1.1) AND Node 20 (prints the 'needs Node 24' message from bin.ts, not 'could not determine executable'). NOTHING of this is started; #471 is the gate", "Draft GitHub release v0.1.0 exists, unpublished: https://github.com/amroja-biz/rockysurf/releases/edit/untagged-7026449c0f2858233dad — the owner has not reviewed it; 0.1.1 needs its own short note pointing at #470", "The 0.0.1 placeholder on npm is still undeprecated. `npm deprecate rockysurf@0.0.1 \"...\"` (owner, 2FA) would give Node-20 users a hint even before 0.1.1; the owner declined 'for today' workarounds but did not say no to the deprecate itself", "#466 (quickstart doc: SSH key write uses the wrong shape and returns 400; no port/data-dir flags documented; credentialCheckNeeded undocumented; secret.key banner unmentioned) — filed, not started; the first item means an agent following the doc cannot install an SSH key", "#467 (Settings field help says save pushes the allow-list; API says sync needed) — filed, not started", "#463 help over MCP — filed, not started", "DigitalOcean is distributed two ways: the GitHub release provider-digitalocean-v0.1.0 (tarball the Shop downloads, sha256 in providers.json) AND @rockysurf/provider-digitalocean on npm since last night. Not a bug; needs an owner decision on whether the Shop should point at npm and per-provider GitHub releases stop", "The npm page for `rockysurf` shows the stale pre-publish README until 0.1.1 ships (READMEs freeze into the tarball); all packages/*/README.md are clean on main as of 79e9925, so 0.1.1 fixes it for free", "Owner's e2e test of 0.1.0 under Node 24 was in progress when this was written; findings, if any, have not been reported", "Carried from predecessor: contribute-* skill live acceptance runs; #437 #438 doc trims; #453 and #459 flakes (#459 hit #471's first CI run today, green on rerun); drizzle-kit db:generate broken since #356 (unfiled); minted local API token to revoke; stash 'On fix/ui-audit-packs'; 22 stale local branches; grok 2026-09-01 and Amp sessions unexamined"]
RECOMMENDED_NEXT: Cut 0.1.1 the moment #471 is on main — the published quickstart command fails for anyone on Node 20 and the owner has said today is the ship date — then verify the Node 20 path by hand before telling the owner to test, because that is the exact case every prior check skipped.
SCOPE_COMPLETE: false
PREDECESSOR: .pass-along/2026-09-07-1551-PASS-ALONG.md
PRIME: ["cd ~/code/rockysurf && git pull && git log --oneline -8", "gh pr view 471 --json state,mergeCommit   # the gate for 0.1.1", "npm view rockysurf version dist-tags   # 0.1.0 until 0.1.1 ships", "sed -n 1,80p docs/RELEASE_SOP.md   # the release procedure, step by step", "gh issue view 464 --comments | tail -8   # the owner's primitives ruling", "gh release list   # the v0.1.0 draft and the DO tarball release", "pgrep -fl 'packages/rockysurf/dist/bin.js' | grep -v mcp   # owner STOPPED their server today; expect nothing"]
---

## Start here

Main is `79e9925`, clean, in sync. One PR open: #471, green, the fix for the bug the owner hit in
their first e2e run. **The owner wants v0.1.1 shipped today.** Read `docs/RELEASE_SOP.md`
(merged this morning, #465) and follow it once #471 is merged. Predecessor:
`.pass-along/2026-09-07-1551-PASS-ALONG.md`.

## May have moved since I stopped

- **#471 may be merged.** `gh pr view 471 --json state`. If merged, the release branch is the
  next act and nobody has started it.
- **The owner was mid e2e test** of `npx -y rockysurf@0.1.0` under Node 24 (they had stopped their
  own server, pid 93424, to free port 3000 and the data dir). Ask what they found before cutting
  0.1.1; a second blocker should ride the same release.
- **The v0.1.0 draft release** may have been published or edited by the owner.
- **Issue #464** stays open on purpose: memory (#468) and sweep (#469) merged, but the issue's
  acceptance list is the owner's to tick. Close it or ask.

## What this session did

1. **Recap from the predecessor**; three post-publish PRs (#460 #461 #462) had landed after it.
2. **Answered the owner's four npm questions** (in chat, not in a doc): release run #2 red only on
   the two-minute smoke timeout after a successful publish (#460 fixed); the npm README is the
   tarball's README, frozen at publish; the provenance "taking too long" line is npmjs.com's UI
   fetch, attestations verify with `npm audit signatures`; DigitalOcean is in the repo and on npm
   because it is maintained here and npm is how a personal Provider is installed (ADR-0026).
3. **`docs/RELEASE_SOP.md`** — agent release-sop, google-doc-style-guide skill, PR #465. The
   owner's reviewer then critiqued the semver policy; all six points applied: 0.x regime (breaking
   → minor before 1.0.0), changed defaults and raised Node minimum are breaking, lockstep caveat
   and the SDK "frozen v0 contract" vs package version, pre-releases `X.Y.Z-rc.N` under dist-tag
   `next` (**release.yml now passes `--tag next` for hyphenated tags**), a **greater-than guard
   step** in release.yml (`npx --yes semver@7 -r ">$cur"`; pre-releases exempt), and a support
   policy (latest only, no backports; deprecate-in-minor-remove-in-major from 1.0.0).
4. **v0.1.0 release notes** — agent release-notes; draft release created, not published. Two
   security claims narrowed while verifying: only *cloud* credentials are env-var-by-name (GitHub
   tokens are literals, ADR-0007 cl. 4); SSH allow-list is AWS/Azure/GCP only (Hetzner has no
   cloud firewall in v0.1).
5. **Agent quick start re-tested** against the published package by a fresh agent from an empty
   dir: install path works; five defects → #466 (doc) and #467 (UI field help).
6. **Primitives**: the owner ruled on #464 — **Provider, Surge Pack, Tool, Server**. Memory
   `docs/memories/2026-09-08-capitalized-product-nouns.md` (#468, written by this session), then
   the sweep (#469, agent primitives-sweep, ~46 min, 133 files, every hit read not regexed, lint
   `scripts/check-primitive-nouns.mjs` wired into `pnpm run lint`). One correction sent back:
   "cloud provider" meaning the vendor stays lowercase; the plugin sense keeps the capital.
7. **#470 found and diagnosed** from the owner's own run: on Node 20, npm's picker skips 0.1.0
   (`engines.node >=24`) and runs the bin-less 0.0.1 placeholder → "could not determine executable
   to run". Reproduced with an empty npm cache; Node 24 works. **#471** drops `engines` from the
   `rockysurf` package only, with a manifest test; the runtime check in `bin.ts` is the guard.
8. Filed #463 (help over MCP) out of #243.

## Commits

Six since `265c390`, all via PR, all merged by the owner: `ec70162` #460, `226d137` #461,
`0f5a30c` #462, `b6c2e06` #465, `6a5416a` #468, `79e9925` #469. Plus `fix/npx-engines-fallback`
(one commit) open as #471.

## Open threads

Neutral inventory; recommendation follows.

- **v0.1.1 release** — in scope (owner: "we are shipping today"), not started, gated on #471.
  Procedure is `docs/RELEASE_SOP.md`. The owner explicitly rejected adding a Node-20 smoke step to
  release.yml for this release ("why not just say Node 24 is a prerequisite") — keep the bump PR
  to the version bump only.
- **v0.1.0 draft release notes** — awaiting owner review.
- **0.0.1 placeholder deprecation** — owner's 2FA; optional; the command is in the #471 comment.
- **#466, #467, #463** — filed, unstarted. #466 item 1 is a real blocker for an agent install.
- **DO dual distribution** — owner decision.
- **#464** — open for the owner's acceptance ticks.
- **Carried** items as listed in OPEN_LOOPS.

## Recommended next — this session's view

I'd cut 0.1.1 as soon as #471 is on main, because the README's one-line quickstart fails for
anyone whose default Node is 20 and the owner has named today as the ship date. Then, before
telling the owner to test, I'd run `npx -y rockysurf@0.1.1 --version` under Node 20 *and* 24 by
hand, since the old-Node path is exactly the one every check before today skipped. Skipping the
Node 20 verification risks shipping a second release with the same class of blind spot.

## Stated direction

- "We are shipping today so my tests need to reflect what I ship." — 0.1.1 today; no "for today"
  workarounds.
- "I just want a clean release." — the bump PR carries nothing but the bump.
- "Why not just say Node 24 is a prerequisite?" — the README already does; no extra workflow step.
- On the primitives (#464): "Provider, Surge Pack, Tool, Server (meaning, the cloud VM)."
- "Having Rocky Surf help available from the MCP is a good idea… requires its own issue." — #463.

## Decisions and rules

- **Capitalized primitives** — memory `2026-09-08-capitalized-product-nouns.md`; lint
  `pnpm run check:primitive-nouns` is in `pnpm run lint`. Vendor senses (identity provider, MCP
  tools, MCP server, the Rocky Surf process) stay lowercase; identifiers/keys/paths never change.
- **Versioning policy** now lives in `docs/RELEASE_SOP.md`: 0.x breaking → minor; lockstep;
  pre-releases under `next`; latest-only support. RELEASING.md keeps the reasons.
- **No `engines` on the `rockysurf` package** (#471, test `package-manifest.test.ts`) — on the
  npx-run package, `engines` hides the version that would explain old Node.
- **Draft releases, never published, for release notes** — the owner publishes.
- **ADR bodies, past memories, `docs/history/`, pass-alongs are never swept** for copy rules.

## Traps

- **npm's version picker honours `engines`.** `npx pkg` on an older Node silently falls back to
  the newest version whose `engines` pass, which for a name claimed by a bin-less placeholder is
  the placeholder. Every check of the published CLI ran on Node 24, so nobody saw it.
- **npx caches per spec.** Tell testers to pin (`rockysurf@0.1.1`) or they may get a cached
  older resolution.
- **#459 (SDK build race) hit #471's first CI run** — `provider-sdk/dist/index.js` not found in
  `bin.e2e.test.ts`. Green on rerun; not the PR's fault. Check the error text before debugging.
- **A judgement sweep is slow.** 133 files took the Opus agent ~46 min; the owner asked twice if
  it was stuck. Say the expected duration up front or split docs from web/CLI.
- **The `.claude/` untracked dir** in the working tree is the agent-worktree parent; ignore it.
- **`cd` into an agent worktree resets the harness's cwd**; use `git -C <path>` instead.

## Waiting on a human

1. Merge #471; merge the 0.1.1 bump PR when it exists; approve the `npm` environment for v0.1.1.
2. Review and publish the v0.1.0 draft release.
3. Optional: `npm deprecate rockysurf@0.0.1 "..."` (2FA).
4. Decide the DO dual-distribution question (Shop tarball release vs npm).
5. Tick or close #464. Carried: revoke the minted local API token; delete the stash and stale
   branches.

## Housekeeping

- **Worktrees**: sweep worktree `agent-a7d182c92336dcbd3` removed at write time; none left.
- **Owner's server**: STOPPED by the owner today for the e2e test. Do not assume port 3000 is
  free either; check.
- **Scratchpad** (`/private/tmp/claude-501/-Users-johndamask-code-rockysurf/a5625b2d-…/scratchpad`):
  `AGENT-BRIEF.md`, `v0.1.0-release-notes.md`, `quickstart-run/` (the dry run's isolated data dir
  and MCP config; safe to delete).
- **Stash**: `On fix/ui-audit-packs` (carried). **Beads**: 0 open.
