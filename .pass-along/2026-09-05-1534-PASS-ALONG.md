---
DATE: 2026-09-05T15:34:20-0400
TITLE: #394 shipped (Surge Packs tab back, in-app provider installer deleted, Settings points at the shop repo, ADR-0028 amended); the five UAT issues written to the owner's Notion Human Board; the "another core is running" lock message diagnosed as the owner's own morning server
STATUS: milestone_complete
MODEL: claude-fable-5-1
MODEL_EFFORT: high
HARNESS: claude-code 2.1.261
SESSION_ID: 3a190abd-324a-4494-9790-dd9ed3ebb42f
REPO: amroja-biz/rockysurf
WORKING_DIR: /Users/johndamask/code/rockysurf
BRANCH: main
HEAD_SHA: 27a083c
UPSTREAM: origin/main (in sync, 0/0)
WORKTREE: clean (agent worktree for #394 removed; owner's checkout at 27a083c)
ISSUE_TRACKER: GitHub
ISSUES: ["closed this session: #394 (PR #395; companion amroja-biz/rockysurf-shop#15)", "open, OWNER UAT, now mirrored on the Notion Human Board: #372 #373 #375 #376 #377", "owner-filed, still unexamined: #382 #383 #384 #386", "inherited/open: #364 #340 #339 #337 #315 #275 #261 #253 #243 #167 #166 #165 #136 #134 #133 #131 #130 #123 #104 #103 #102 #101 #98 #78 #77 #76 #75"]
TASK_TRACKER: Beads
BEADS: ["0 open, 34 closed — none created this session (single issue, no epic)"]
PRS: ["none open in either repo; #395 and rockysurf-shop#15 both merged by the owner"]
OPEN_LOOPS: ["Owner UAT #372/#373/#375/#376/#377 still unrun; #372–#376 need a DigitalOcean account + token (none on this machine; with-key has no DO entry); #377 needs no token and can run today against the restarted server", "#375/#376 test bodies on GitHub still describe the in-app shop listing/installer that #395 deleted — the Notion rows were rewritten for the command-line flow, the GitHub issues were NOT", "#315 flip (UI (browser) → required check): precondition met by #379, flip not done, #315's own rule wants a clean streak first — #395's browser job was green", "drizzle-kit db:generate broken since #356 — still unfiled, hand-write migrations", "Dependabot fast-uri (high) + qs (medium) in pnpm-lock.yaml — still unaddressed", "#372's title still says 'adding-providers' (skill is add-provider since #388) — cosmetic", "Cursor extension-host process for the rockysurf-website workspace carries cloud/API credentials in its environment (inherited from the shell profile) — reported to the owner, no action taken", "stash 'On fix/ui-audit-packs' still present — superseded since 2026-09-02, safe to drop, owner's call (fifth session carrying this)", "local branches pass-along/2026-09-04 and pass-along/2026-09-05 are merged and deletable", "grok session 2026-09-01 (01a059bd…) and Amp T-01a01aae still unexamined (carried)"]
RECOMMENDED_NEXT: Run #377 (declared-settings Settings page) against the freshly restarted server since it needs no DO token, and re-word the GitHub bodies of #375/#376 to match the post-#394 command-line install flow before anyone runs them; #372 remains the real acceptance test once a DO token exists.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-09-05-1146-PASS-ALONG.md
PRIME: ["cd ~/code/rockysurf && git pull && git log --oneline -10", "gh issue list --repo amroja-biz/rockysurf --state open --limit 40", "cat docs/memories/llms.txt", "pgrep -fl 'packages/rockysurf/dist/bin.js'  # is the owner's server up, and which build"]
---

## Start here

Main is `27a083c`, clean, in sync, zero open PRs in both repos, zero open beads, no agent
worktrees. One issue shipped this session (#394 → PR #395 + rockysurf-shop#15), both merged by
the owner. The owner's server was restarted at 15:27 EDT on this build (pid 56918 at the time of
writing). Predecessor: `.pass-along/2026-09-05-1146-PASS-ALONG.md` — read it for the #294
provider-model state; nothing there has changed except what #394 revised.

## May have moved since I stopped

- **The owner may have run UAT.** The five UAT issues now live on the Notion "Rocky Surf Human
  Board" (data source "Rocky Surf Human Tasks", rows "UAT 1"–"UAT 5", all "Not started"). If a
  row has moved to Done or In progress, the owner did it by hand; the GitHub issue may lag.
- **The owner's MCP client** needed a reconnect after the 15:27 restart (tool list builds at
  process start). If MCP calls look stale, that is why.
- **Tonight's scheduled nightly (~12:00 UTC 2026-09-06)** is the first to run on a main that no
  longer has the shop-provider installer (#395 deleted `providers/{install,tarball,shop,
  shop-index,shop-routes}.ts` in core). It should be unaffected — nothing e2e touched that path —
  but a red there is the first place to look.

## What this session did

1. **Recap** from the 11:46 pass-along; found #394 filed by the owner at 18:31Z after it.
2. **UAT → Notion.** Five rows created on the Human Board (`collection://3bac341c-8476-80fa-96dd-000bfcb92c22`),
   one per issue #372/#373/#375/#376/#377, each with the GitHub link and the steps; UAT 1–4 note
   the DO-token prerequisite; UAT 3/4 were rewritten after #395 merged to describe the
   command-line install flow (the in-app installer no longer exists).
3. **#394 → PR #395** (`27a083c`), one Opus agent in a worktree, ~18 minutes, no owner questions
   needed. Tab/nav/tab-strip back to "Surge Packs" (route unchanged); the Providers tab and the
   entire in-app installer deleted as dead code (core `providers/{install,tarball,tar.fixture,
   shop,shop-index,shop-routes}.ts` + tests, routes unmounted, `countServersOnProvider` gone,
   `safe-fetch.ts` back to text-only; web `ProviderShop`, API client, CSS, e2e); a one-line
   pointer to the shop repo's providers section on each provider tab of Settings; ADR-0028
   amended in place (decisions 3 and 5 superseded, 1/2/4 held with the operator as actor);
   `docs/adr/llms.txt`, `self-hosting.md`, `writing-a-provider.md`, `writing-a-pack.md`,
   `SECURITY.md`, `build-bundled-package.mjs` updated. Personal providers in Settings needed no
   fix — #366/#367/#379 already render them like official ones, pinned by
   `settings-personal-provider.e2e.ts` and `settings-declared-clouds.e2e.ts`. Two new browser
   tests. Diff +323/−3400 across 36 files; 58/58 checks green incl. `UI (browser)`.
4. **rockysurf-shop#15** — README providers section rewritten with the command-line install
   steps ("Installing one" subsection) that `SHOP_PROVIDERS_URL` in the app now targets. Merged
   after #395, in the right order.
5. **Lock-message diagnosis** (read-only): the owner's `./start.sh` refused with "another
   rockysurf core (pid 25858) is already running". Pid 25858 was `node packages/rockysurf/dist/bin.js`
   (the `exec` line of start.sh — relative path, no `mcp` arg), started 10:51 local from
   `~/code/rockysurf`, parent launchd. It was the owner's own morning server, orphaned when its
   terminal closed. Neither agents nor the MCP spawned it: the MCP entry in `~/.claude.json`
   runs `bin.js mcp` with an absolute node path, and the MCP never starts a core (#348).
6. **Cleanup**: agent worktree removed, local + remote fix branches gone, main pulled.

## Commits

2 since `c01adb1`: `b6dff06` (predecessor pass-along, #393), `27a083c` (#394 via #395). This
pass-along rides its own PR branch (main is PR-only).

## Open threads

Neutral inventory; recommendation follows.

- **Owner UAT, all unrun** (in scope only as "get it onto the board", which is done): #372
  (DO via the skill — the real acceptance test), #373 (droplet lifecycle), #375 (publish DO to
  the shop registry), #376 (install DO from the shop on a fresh install), #377 (declared-settings
  page). #372–#376 need a DO account + token; #377 does not.
- **#375/#376 GitHub bodies are stale after #394.** They say "open the shop in a Rocky Surf
  instance … install the DO provider" — the in-app listing and installer are gone. The Notion
  rows say the right thing; the GitHub issues were not edited (no ruling asked, none given).
- **#315 flip** — precondition met (#379); not done; #395's `UI (browser)` was green, one more
  data point toward the streak #315 wants.
- **drizzle-kit generator broken since #356** — unfiled, third pass-along carrying it.
- **Dependabot** fast-uri (high) + qs (medium) — unaddressed, offered twice.
- **Owner-filed, unexamined**: #382 (Actions + node 20), #383 (document testing strategy),
  #384 (remove beads viewer from packs), #386 (trademark.md); inherited #364 #339 #340 #337.
- **Credentials in a Cursor process environment** — the owner's Cursor extension host for the
  rockysurf-website workspace carries several cloud/API tokens as env vars (from the shell
  profile). Flagged to the owner in chat; values were never printed or written anywhere. Their
  call whether to move those exports behind `with-key`.
- **Cosmetic**: #372's title uses the old skill name.
- **Stash** superseded; **grok/Amp sessions** unexamined; **merged pass-along branches**
  `pass-along/2026-09-04` and `pass-along/2026-09-05` still exist locally.

## Recommended next — this session's view

I'd run #377 now: it needs no DigitalOcean token, the server was just restarted on a build that
includes everything it tests (#366/#367/#379 declared settings, plus #395's Settings pointer),
and the agent's Playwright PASS has never had a human look. Then re-word #375/#376 on GitHub so
whoever runs them follows the command-line flow rather than hunting for a Providers tab that no
longer exists — a small edit, but the Notion rows and GitHub issues currently disagree. #372
stays the highest-value test overall and is gated only on a DO token. Everything else open is
owner-paced.

## Stated direction

- "write UAT to my Notion board … The[n] get to work on 394. Spin up an agent for this" —
  done as stated; one agent, not a team.
- The owner merged both PRs themselves; no delegation of merges this session.
- No direction given on UAT order, #315, drizzle-kit, Dependabot, or the stash.

## Decisions and rules

- **Providers are pointed at, not installed, from the app** (#394 → ADR-0028 amendment).
  The shop repo still distributes providers via `providers.json`; installation is a command-line
  step documented in the shop README; the app's only shop-provider surface is the one-line
  pointer on each provider tab of Settings. Do not reintroduce an in-app installer.
- **The tab is "Surge Packs".** "The shop" as a noun still means the community registry repo
  (`amroja-biz/rockysurf-shop`), not a page in the app.
- **Personal and official providers render identically in Settings** — declared settings
  (ADR-0027) is the only path; a typo'd `package` yields a writable field, not a silent drop.
- **A "core already running" lock message is almost always the owner's own server** — check
  `ps -o pid,lstart,command` and `lsof -d cwd` before suspecting an agent or the MCP. The MCP
  command line always carries the `mcp` argument; `start.sh`'s never does.

## Traps

- **Two PRs in two repos look like one to the owner.** The agent opened #395 (rockysurf) and
  #15 (rockysurf-shop); the owner saw only the rockysurf PR list and asked where the second was.
  When a change spans repos, name both repos and both PR URLs on separate lines, and say which
  repo each lives in.
- **`gh pr checks` output is not a clean pass/fail table** for the pack-smoke matrix; trust
  `mergeStateStatus: CLEAN` plus the agent's per-check report rather than parsing check lines.
- **`pgrep -fl` on this machine prints full environments of some processes** (Cursor helpers
  include their env in the command line). Pipe through `cut` or filter before it reaches
  context, or credentials land in the transcript.
- Everything in the predecessor's Traps still holds (idle notifications carry no content;
  agents stop to wait unless told not to; runner starvation looks like red).

## Waiting on a human

1. A DigitalOcean account + token (`with-key` name `DIGITALOCEAN_TOKEN`) — blocks #372/#373/#375/#376.
2. Go/no-go: re-word #375/#376 on GitHub; file the drizzle-kit issue; bump the two Dependabot alerts.
3. Move the Cursor-visible credentials behind `with-key`, or accept them as-is.
4. #315 flip timing; scope #382 #383 #384 #386.
5. Drop the superseded stash and the two merged local pass-along branches.

## Housekeeping

- **Worktrees**: none (`git worktree list` = main only). **Beads**: 0 open.
- **Owner's server**: pid 56918, `node packages/rockysurf/dist/bin.js` from `~/code/rockysurf`,
  started 15:27 EDT on `27a083c`. The lock at `~/.rockysurf/core.lock` is held by it.
- **Scratchpad** (this session, `/private/tmp/claude-501/-Users-johndamask-code-rockysurf/3a190abd-…/scratchpad`):
  `AGENT-BRIEF.md` updated through the #394 ruling and the 2026-09-05 landed list — copy it
  forward. Nothing else.
- **Notion**: five UAT rows on the Human Board; UAT 3/4 already reflect the post-#394 flow.
- No cloud resources were created by anyone this session. Nothing exists on DigitalOcean.
