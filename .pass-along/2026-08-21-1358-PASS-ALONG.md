---
DATE: 2026-08-21T13:58:23-0400
TITLE: Four PRs merged (tunnel, credential resilience, server metadata ×2); rockysurf-open archived, beads now laptop-only
STATUS: milestone_complete
MODEL: claude-fable-5
MODEL_EFFORT: high
HARNESS: claude-code 2.1.228
SESSION_ID: 428d8d4f-5624-4682-9f0a-db75bc84fb40
REPO: amroja-biz/rockysurf-open
WORKING_DIR: /Users/johndamask/code/1-open-source-rocky-surf-v0-1
BRANCH: 1-open-source-rocky-surf-v0-1
HEAD_SHA: 221ee47
UPSTREAM: origin/1-open-source-rocky-surf-v0-1 (in sync at archive time; repo is now READ-ONLY)
WORKTREE: clean
ISSUE_TRACKER: GitHub
ISSUES: ["rockysurf#46 (closed by PR #47)", "rockysurf#2 (loud launch, open)"]
TASK_TRACKER: Beads
BEADS: ["rockysurf-jv0o (P1 bug, open, owner-DEFERRED — but its failure mode killed the nightly twice since, see comment)", "rockysurf-31du (P1, launch-day CI migration, open)", "rockysurf-bbmi/gg9x/mg8w/idxd/vbma (all closed this session)", "rockysurf-4aed (ready, carried)", "rockysurf-zn33 (P3 flaky test, open)"]
PRS: []
OPEN_LOOPS: ["owner's core at ~/code/rockysurf still runs a906c4e — four merged PRs and migrations 0009/0010 not picked up until they pull+rebuild+restart", "nightly real-cloud FAILED Aug 20 AND Aug 21: both AWS legs, arm64 dies at tool:build-essential 'Unable to fetch some archives' — jv0o's exact failure mode, deferred by owner the same day", "beads DB has no git home — bd dolt push fails against the archived rockysurf-open; owner is deciding where beads lives next", "launch gates unchanged: m390 npm account, o45s.2 stranger test, o45s.3 v0.1.0 release, 31du CI secrets/OIDC migration", "carried: arym.13, hgz6, 6a2s, zn33, px4b, 4aed, Pack Shop nav ruling", "two stale node processes from older sessions still running (PIDs 59393, 40668)"]
RECOMMENDED_NEXT: Ask the owner to re-decide jv0o — its failure mode has now failed the nightly two mornings running, which is new evidence postdating their 'ignore jv0o'; second, get their core restarted onto a834565 so the four merged fixes actually reach the UI they test in.
SCOPE_COMPLETE: true
PREDECESSOR: .pass-along/2026-08-19-2257-PASS-ALONG.md
PRIME: ["bd prime", "cd ~/code/rockysurf && git log --oneline -5", "gh run list --repo amroja-biz/rockysurf --workflow nightly-real-cloud.yml --limit 3"]
---

## Start here

All development now happens in the PUBLIC repo `amroja-biz/rockysurf` (main at `a834565`,
all green). This private repo is ARCHIVED on GitHub — read-only, no more pushes, kept only
as history. Beads still works but is laptop-only. Boot with `bd prime`; build in worktrees
off `public-launch/main` (remote already configured in this checkout).

## May have moved since I stopped

- **The nightly is red and getting redder.** `nightly-real-cloud.yml` on the public repo
  failed 2026-08-20 07:41 UTC and 2026-08-21 07:43 UTC — both AWS legs. The arm64 log dies
  at `tool:build-essential`, `E: Unable to fetch some archives` — **that is rockysurf-jv0o's
  exact failure mode**, which the owner deferred ("ignore jv0o") the morning of the 20th,
  before these two failures existed. Evidence is on the bead. Also seen in the log: an
  `::error::the nightly is running as '$arn', not the role deployed from
  deploy/aws/iam-role.yaml` line — check whether the role-identity guard is ALSO failing or
  whether that echo is just the guard's definition being printed; I did not disentangle it.
  A "this run left instances behind; the sweep cleaned up" line means check for orphans if
  investigating.
- **The owner's runtime is stale.** Their core (`~/code/rockysurf`, serving :3000) was last
  rebuilt at `a906c4e` (Aug 20, 10:57). Everything this session merged — tunnel UI,
  credential-expiry notices, rename/description, pack link + Installed card — is invisible
  to them until pull + rebuild + restart. Migrations 0009 (packs.web_port) and 0010
  (servers.description) apply on that restart.
- An unverified Amp session touched something on 2026-08-19 11:46 (workspace unverified —
  may be another repo entirely).

## What this session did

Four PRs, all merged to public main with all 25 checks green each:

- **PR #44** (`10aad3d`, bead `rockysurf-bbmi` closed): packs declare `webPort` (optional,
  1–65535); Connect renders the `ssh -L` command + localhost URL for declaring packs (the
  `requiresRdp` pattern); the guide hint stopped claiming every step runs on the box;
  `deepseek-harness` declares 3080. Format docs + skill contract updated; migration 0009.
- **PR #45** (`7d98529`, bead `rockysurf-gg9x` closed): `lifecycle.get/list` degrade instead
  of throwing when a provider can't be asked — stored rows served with the provider's own
  message as `syncError` (rides inside `present()`); SPA shows one notice per affected
  provider with the remedy verbatim. Born from a live incident: the owner's GCP ADC hit
  Google's periodic reauth (`invalid_rapt`) and GET /servers 500'd the whole dashboard.
- **PR #47** (`3e0342b`, bead `rockysurf-mg8w` closed, closes issue #46): servers renameable
  + optional description via `PATCH /api/v1/servers/:serverId` (`lifecycle.updateMetadata`,
  display-only — provider identity is the row id); description at create; Surge Pack named
  in the detail summary; migration 0010.
- **PR #48** (`a834565`, bead `rockysurf-idxd` closed): owner feedback on #47 — pack name
  links to `/admin/surge-packs`; Installed card falls back to the pack's tool list (linked,
  described, with a drift caveat) because every SPA-created box has `server.tools == []`,
  which had rendered the card as a heading over nothing.

Non-code:

- **rockysurf-open retired**: all three workflows disabled (Actions quota — see Decisions),
  then the whole repo ARCHIVED at the owner's direction. Beads memory
  `github-actions-quota-2026-08` records the full story and consequences.
- **vbma closed** (owner testimony: zero 500s on current build = stale-build confirmed);
  **jv0o deferred by owner** (but see May-have-moved); **7fyf** got the owner's Connect
  GitHub end-to-end verdict (worked, no PAT needed).
- Diagnosed the "Could not load your servers" 500 to expired GCP ADC (curl'd the API with a
  minted token; the error named the fix). Owner re-authed; gg9x built from the diagnosis.

## Commits

This repo: `221ee47` (three Aug-19 pass-alongs, checkpoint, graph snapshot) — pushed before
the archive. Public repo: the four merge commits above. Nothing unpushed anywhere.

## Open threads

Neutral inventory; recommendation follows.

- **jv0o** (P1, open, owner-deferred): apt retry + mirror fallback in bootstrap. NEW
  EVIDENCE postdates the deferral — nightly failed twice with its exact signature. Needs the
  owner to re-decide, not a unilateral build.
- **Nightly red** (no bead yet beyond jv0o's comment): if the failures are all
  mirror-fetch, jv0o covers it; if the IAM-role guard line is real, that's separate — the
  31du secrets/OIDC work may be implicated. Undiagnosed beyond the log lines quoted above.
- **Owner core restart** — blocked on the owner; everything merged is invisible until then.
- **Launch gates** (in `rockysurf-o45s`): m390 (npm account, owner-gated), o45s.2
  (stranger test), o45s.3 (v0.1.0 + launch post), 31du (CI secrets/OIDC follow the repo
  move). o45s.1 (hero GIF) is in progress from earlier sessions.
- Carried, unchanged: arym.13 (fork-PR disclosure), hgz6 (curl|bash in shipped packs),
  6a2s (bd -C hazard), zn33 (flaky SettingsPage test), px4b (GHES host), 4aed (nightly GCP
  leg), 4zmx, aiqu, u5g6, and the P3/P4 tail (33 open total — `bd list --status open`).
- **Pack Shop nav ruling** — still open from two sessions ago.

## Recommended next — this session's view

1. **Put jv0o back in front of the owner.** They said "ignore jv0o" before the nightly
   failed twice with its exact failure mode; that decision was made on evidence that has
   since changed. If they reverse, the bead carries the full fix shape (apt retries + bounded
   fallback to the global mirror, loud in the log). Consequence of skipping: every arm64 AWS
   create — including a launch-day stranger's first one — dies whenever Canonical's EC2
   mirror hiccups, and the nightly stays red, masking any new regression behind a known one.
2. **Get the owner's core onto `a834565`** — until then they're testing a build that
   predates everything merged this session, which is how the vbma-style confusion starts.
3. Then the launch gates, which are mostly owner-gated anyway.

## Stated direction

- "bbmi yes, ignore jv0o" — bbmi built; jv0o deferred (pre-nightly-failures).
- "commit and sync for the repo" — done, then superseded by:
- "I don't want that repo to stay around... rocky surf open, including the beads, is all
  archivable" — archived, beads left laptop-only with no replacement remote, per their
  explicit "can't think of a reason to preserve beads history".
- "Plan and build issue 46" — done (PRs #47, #48).
- The Surge Pack "should be linked. Either that or show the contents in the Installed card"
  — both done in #48.

## Decisions and rules

- **Beads is laptop-only by owner decision.** `bd dolt push` now FAILS (archived remote).
  Do NOT treat that as a bug, do NOT create a replacement sync repo without asking. Memory
  `github-actions-quota-2026-08` is the durable record.
- **Only private repos consume GitHub Actions included minutes.** The 90%-quota email was
  ~1,792 min burned Aug 12–13 in rockysurf-open pre-launch; the public repo's big CI runs
  are free. All rockysurf-open workflows are disabled and the repo archived — nothing can
  spend quota now. Resets Sep 1.
- **Rename is display-only** (issue #46): the provider identity is the row's hostname-safe
  `id` (schema C2 comment). Don't add provider calls or status rules to `updateMetadata`.
- **`syncError` rides inside `present()`**, not spread in at the route — the
  DashboardPage.wiring row-shape guard regex requires every `Server` interface field to be a
  literal 4-space-indented `key:` line in `present()`'s return. New server fields must
  follow or the guard fails.
- **Reads degrade, actions refuse** (gg9x): get/list serve stale rows with the provider's
  message; start/stop/terminate still throw. Keep that split.
- **PR bodies and commits carry no Claude/Anthropic attribution** (user's global rule; I
  slipped once on PR #47 and edited it out).

## Traps

- **This workspace needs node 24** — corepack pnpm crashes on the default node 20
  (`ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING`). `. $NVM_DIR/nvm.sh && nvm use 24` in every
  shell that runs pnpm.
- **Fresh worktrees need `pnpm install` AND `pnpm -r build`** before tests: unbuilt
  `@rockysurf/provider-sdk` fails vitest resolution, and `bundle-assets.test.ts` (18 tests)
  reads `packages/web/dist` — its failures mean "you didn't build", not "you broke it".
- **bd resolves its workspace to `~/code/rockysurf-open`**, not this checkout — the Dolt DB
  lives there (`.beads/embeddeddolt`). Related standing bug: 6a2s.
- **`echo ===X===` breaks zsh** (`== not found`) — quote separators in chained commands.
- **Shell cwd resets between Bash calls** here; `cd` inside each command or use absolute
  paths.
- The GCP provider's auth error message contains the literal fix command — render/relay it
  verbatim; don't paraphrase provider errors.
- An arm64 bootstrap failure at `tool:build-essential` is a mirror problem until a curl of
  the failing URL proves otherwise (predecessor's trap, now CI-confirmed).

## Waiting on a human

- **jv0o re-decision** in light of two nightly failures (owner said ignore; evidence moved).
- **Owner core restart** onto `a834565`.
- Launch gates: npm account claim (m390), stranger-test recruits (o45s.2), the v0.1.0 nod.
- Pack Shop nav ruling (carried).

## Housekeeping

- All three build worktrees (bbmi, gg9x, i46, i46b) removed; local branches deleted; remote
  branches remain on the public repo (merged, prunable).
- Two stale node processes from OTHER sessions still run: PID 59393
  (`packages/rockysurf/dist/bin.js`, config in another session's scratchpad `run-fixed`)
  and PID 40668 (running since Aug 12, temp CLI config). Neither is this session's; both
  look safe to kill but confirm with the owner.
- The owner's live core is PID 60031 (`~/code/rockysurf`, :3000, tty s040) — leave it alone.
- Pass-alongs moved house (owner ruling, after this file was first written): this one and
  all FUTURE pass-alongs are committed to `.pass-along/` in the PUBLIC repo
  `amroja-biz/rockysurf` — they are world-readable, so the no-secrets rule is now
  load-bearing. Older pass-alongs stay behind in the archived rockysurf-open and were
  deliberately not migrated. Beads' next home is a separate, still-open decision.
