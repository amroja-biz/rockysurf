Mount once, as the first child of the page, before any other etched part.

```jsx
<AppShell title="Servers"><EtchedDefs />…</AppShell>
```
It renders a 0×0 svg holding `#rs-hatch` and `#rs-hatch-cross`. Two copies on one page duplicate the ids — mount it once.
