<Badge variant="trust-official">official</Badge>
<Badge variant="run-as-root">runs as root</Badge>
<ArchBadge arch="arm64" />

```jsx
Never invent a ranking between community and internal — they share one style deliberately.
```
