Sits beside the uptime value, never instead of it. Uptime has no ceiling, so it must not be drawn as a gauge — a stacked curve or a filled bar implies an axis that does not exist. A tally counts, which is what "2h 14m" does.

```jsx
<dd><Tally hours={6.03} /></dd><dd>6h 02m</dd>
```
