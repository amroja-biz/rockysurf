<CheckboxRow checked={force} onChange={setForce}>Create anyway — I know one of these repositories will not clone.</CheckboxRow>
<Chip onClick={add}>github.com/you/api</Chip>
<Chip state="added" disabled>github.com/you/web</Chip>

```jsx
undefined
```
