A dismissible yellow banner for advice the operator should read once — longer copy than IpChangeAlert, so content and dismiss sit on their own rows.

```jsx
<BackupReminder onDismiss={hide}>
  Back up <code>~/.rockysurf</code> — the database and the key are useless without each other.
</BackupReminder>
```
