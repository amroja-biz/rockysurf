Wraps a page block; Inset is a box one step deeper inside it (a log, a token entry).

```jsx
<Card title="Connect"><pre><code>ssh -i dev-box.pem rocky@203.0.113.10</code></pre></Card>
```
Never add a shadow, and never a coloured left border except the purple agents callout.