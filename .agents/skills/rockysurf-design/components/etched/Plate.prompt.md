Replaces Card and ServerCard's frame in the etched skin. Nothing inside changes; only the boundary does.

```jsx
<Plate lit>{…}</Plate>
```
Reserve `lit` for state that is genuinely live — a running box, the current step. Every plate lit is no plate lit.
