Only for a quantity whose ceiling is named next to it — spend against the cap is the one in this product. On a card there is no room to state the cap, so the cost stays a plain number there.

```jsx
<Waterline fraction={0.34} width={260} />
<p className="hint">$17.04 of $50.00 this month</p>
```
