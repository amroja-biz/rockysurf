<Tabs tabs={[{id:'installed',label:'Installed'},{id:'shop',label:'Shop'}]} value={tab} onChange={setTab} />

```jsx
Never box the active tab — a boxed tab beside a boxed radio card is two competing rectangles.
```
