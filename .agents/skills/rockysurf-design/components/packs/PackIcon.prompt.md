Every place a pack is named. A card never has a hole where its mark should be.

```jsx
<PackIcon pack={{ packId: 'open-claw', name: 'Open Claw', imageUrl: '../../assets/surge-packs/open-claw.png' }} />
<PackIcon pack={{ packId: 'rust-dev', name: 'Rust Dev' }} size="large" />
```
