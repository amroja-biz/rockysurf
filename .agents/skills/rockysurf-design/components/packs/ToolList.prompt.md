The Installed card on a server, and the contents of a pack on /packs/:packId.

```jsx
<ToolList tools={[{ toolId: 'claude-code', name: 'Claude Code', url: 'https://claude.ai/code', description: 'Anthropic\'s CLI harness' }]} />
```
PackGuide renders pack-authored text inside a <pre> — never dangerouslySetInnerHTML.