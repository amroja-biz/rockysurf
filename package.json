{
  "name": "rockysurf",
  "private": true,
  "type": "module",
  "description": "Rocky Surf — self-hosted persistent cloud dev boxes for coding agents.",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "node scripts/check-core-deps.mjs && node scripts/check-iam-policy.mjs && node scripts/check-azure-role.mjs && node scripts/check-gcp-role.mjs && node scripts/check-npx-closure.mjs && node scripts/check-size-table.mjs",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
