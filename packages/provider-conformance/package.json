{
  "name": "@rockysurf/provider-conformance",
  "version": "0.1.0",
  "private": true,
  "description": "SDK conformance checks every compute provider runs against itself. Test-only; never a runtime dependency.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rockysurf/provider-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "_comment": "Resolved from SOURCE, not dist: this package is private, test-only, and never published, so pointing at src removes a build-ordering dependency between provider packages and their own test runs."
}
